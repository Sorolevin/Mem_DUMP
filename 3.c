#include<stdio.h>
	
	int power(int x , int y);
	int a,b;
	int main()	{
				scanf("%d %d",	&a,&b );
				printf("%p",		power(a,b));
		return 0;
	}
	
	int power(x ,y){
		int d;
		for(x; x <= y; x *= x	){
		d = x;
		}
		
	
			
		return x;
		}
		
	
